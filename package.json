{
  "name": "portfolio_nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:legacy": "NODE_OPTIONS='--openssl-legacy-provider' next dev",
    "build": "next build && next export",
    "build:legacy": "NODE_OPTIONS='--openssl-legacy-provider' next build && next export",
    "start": "next start"
  },
  "dependencies": {
    "next": "10.2.3",
    "npm-watch": "^0.13.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-icons": "^4.2.0",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  }
}
